<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: "Ask a question", required: true, placeholder: "How should I can improve my operation based on this conversation?" %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
